{
  "description": "i-have-headache SessionStart hook: concise mode is always on. Exec form (command + args): the shell form exits 126 on Claude Code 2.1.154 under Git Bash.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "sh",
            "args": [
              "${CLAUDE_PLUGIN_ROOT}/hooks/session-start.sh"
            ]
          }
        ]
      }
    ]
  }
}
